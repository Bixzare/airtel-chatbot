# Google API key for Gemini models
GOOGLE_API_KEY=your_google_api_key

# Model name (defaults to gemini-1.5-flash if not specified)
MODEL_NAME=gemini-1.5-flash

# Document path (defaults to static_document.txt if not specified)
DOCUMENT_PATH=src/rag/static_document.txt

# Session timeout in minutes (defaults to 30 if not specified)
SESSION_TIMEOUT_MINUTES=30

# Port for the API (defaults to 8000 if not specified)
PORT=8000 